		<div class='content-block'>
      <h2><%= t ('hot_tours') %></h2>
			<ul class='list-184'>
			<% Destination.order('created_at desc').limit(14).each do |d| %>
        <% if d.description %>
          <li><%= link_to d.description.local_title(locale), destination_path(d), :title => d.description.title %></li>
        <% end %>
			<% end %>
      <div class='clear'></div>
			</ul>
		</div>
